pipeline {
  agent {
    node {
      label 'dckubt16s1'
    }
    
  }
  stages {
    stage('build docker') {
      steps {
        timestamps() {
          sh '''#!/bin/bash

set'''
        }
        
      }
    }
  }
}